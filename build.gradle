plugins {
    id 'java'
    id 'application'
    id 'com.github.johnrengelman.shadow' version '7.1.2'
    id 'checkstyle'
}

checkstyle {
    toolVersion = '10.2' // Specify the version of Checkstyle you want to use
    configDirectory = file("${rootDir}/config/checkstyle") // Set the directory containing the Checkstyle config files
    configProperties = [
            'checkstyle.suppressions.file': file("${rootDir}/config/checkstyle/suppressions.xml") // Set the suppressions file
    ]
}

tasks.withType(Checkstyle) {
    reports {
        xml.enabled = false
        html.enabled = true // Enables the HTML report format
    }
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter-api', version: '5.10.0'
    testRuntimeOnly group: 'org.junit.jupiter', name: 'junit-jupiter-engine', version: '5.10.0'
}

test {
    useJUnitPlatform()
    testLogging.showStandardStreams=true
}

application {
    mainClass = 'Gutti.Gutti'

}

jar {
    manifest {
        attributes 'Main-Class': 'Gutti.Gutti'
    }
    }


run{
    standardInput = System.in
}


