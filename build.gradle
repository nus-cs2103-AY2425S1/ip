plugins {
    id 'java'
    id 'application'
    id 'com.github.johnrengelman.shadow' version '7.1.2'
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter-api', version: '5.10.0'
    testRuntimeOnly group: 'org.junit.jupiter', name: 'junit-jupiter-engine', version: '5.10.0'
}

test {
    useJUnitPlatform()

    testLogging {
        events "passed", "skipped", "failed"

        showExceptions true
        exceptionFormat "full"
        showCauses true
        showStackTraces true
        showStandardStreams = false
    }
}

application {
    mainClass.set("Jay")
}

shadowJar {
    archiveBaseName = "Jay"
    archiveClassifier = null
}

run {
    standardInput = System.in
    standardOutput = System.out
}

tasks.register('cleanTestFiles', Delete) {
    delete file("/data/test1.txt")
    delete file("/data/test2.txt")
    delete file("/data/test3.txt")
    delete file("/data/test4.txt")
    delete file("/data/test5.txt")
    delete file("/data/test6.txt")
    delete file("/data/test7.txt")
}

clean.dependsOn("cleanTestFiles")
