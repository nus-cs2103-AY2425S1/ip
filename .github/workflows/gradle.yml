name: Java CI

on: [push, pull_request]

jobs:
  build:
    strategy:
      matrix:
        platform: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.platform }}

    steps:
      - name: Set up repository
        uses: actions/checkout@v4

      - name: Set up Zulu JDK 17 (with JavaFX)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17.0.11'
          cache: 'gradle'

      - name: Set up headless mode for TestFX (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb
          export DISPLAY=:99
          Xvfb :99 -ac &

      - name: Build and test with Gradle on Linux
        if: runner.os == 'Linux'
        run: xvfb-run -a ./gradlew check --info

      - name: Build and test with Gradle on macOS and Windows
        if: runner.os != 'Linux'
        run: ./gradlew check --info
