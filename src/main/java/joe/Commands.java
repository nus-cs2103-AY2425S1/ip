package joe;

/**
 * The {@code Commands} enum represents the various commands that can be issued
 * by the user in the application. Each enum constant corresponds to a specific
 * command that the application can recognize and execute.
 */
public enum Commands {
   LIST, MARK, UNMARK, TODO, DEADLINE, EVENT, BYE, DELETE, FIND, HELP
}
