package atreides;

import javafx.application.Application;

/**
 * The Launcher class is the entry point for the JavaFX application.
 * It launches the Main class which initializes and runs the GUI for the Duke chatbot.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
