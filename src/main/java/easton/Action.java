package easton;

/**
 * Represents the different types of actions/commands that the chatbot can handle.
 */
public enum Action {
    BYE, LIST, MARK, UNMARK, TODO, DEADLINE, EVENT, DELETE, FIND, INVALID
}
