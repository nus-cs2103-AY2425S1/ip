package bimo.gui;

import javafx.application.Application;

/**
 * Represents a launcher which is the entry point to application.
 */
public class Launcher {
    /**
     * Starts the chatbot.
     *
     * @param args
     */
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
