package utility;

/** An enum class that contains all the command keywords */
public enum Command {
    list,
    find,
    mark,
    unmark,
    delete,
    todo,
    event,
    deadline,
    note,
    deleteNote,
    bye,
}
