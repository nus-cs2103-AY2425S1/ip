package ollie;

/**
 * Represents the type of command parsed from the user's input.
 */
public enum Command {
    GREETING,
    EXIT,
    LIST,
    MARK,
    UNMARK,
    DELETE,
    TODO,
    DEADLINE,
    EVENT
}
