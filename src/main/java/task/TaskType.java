package task;

/**
 * The {@code TaskType} enum represents the types of tasks that can be created.
 * It defines three types of tasks: {@code TODO}, {@code DEADLINE}, and {@code EVENT}.
 */
public enum TaskType {
    TODO, DEADLINE, EVENT
}
