package gui;

import javafx.application.Application;

/**
 * Main class that runs the GUI-based version of the Applemazer chatbot program.
 */
public class Launcher {
    /**
     * Entry point of the GUI-based version of the Applemazer chatbot program.
     * @param args CLI arguments.
     */
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
