package tayoo;

import javafx.application.Application;
public class Launcher {
    /**
     * Runs the start() method within the Main class.
     * @param args
     */
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
